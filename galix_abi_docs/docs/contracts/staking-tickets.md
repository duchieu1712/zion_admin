```bash
_ stake(<amount>) # Deposit token vào contract để stake
_ withdraw(<amount>) # Rút token đã stake về ví
_ getReward() # Rút tickets reward đã kiếm được

_ earned(<account>) # Xem token reward đã kiếm được
_ viewUnstakeTime(<account>) # Xem thời gian có thể unstake
```
