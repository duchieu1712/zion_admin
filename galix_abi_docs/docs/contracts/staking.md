```bash
_ stake(<token>, <amount>) # Deposit token vào contract để stake
_ withdraw(<token>, <amount>) # Rút token đã stake về ví
_ getReward() # Rút token reward đã kiếm được

_ earned(<account>) # Xem token reward đã kiếm được
_ rewardPerToken() # Xem phần thưởng có thể nhận được từ mỗi token đã stake
_viewRemainingReward() # Xem phần thưởng còn lại
```
